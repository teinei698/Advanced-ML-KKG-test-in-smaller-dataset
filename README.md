# Here is the instruct of this project:

We will execute and implement the project in this sequence:

load_data.py: we need to download data packages mentioned in the article

build_graph: reform triple to graphs so as to analysis their relation and connection

negative_sampling: this file is for negative check, because we need a negative group to test our model and see if it can still categorize the group correctly as well

features: the core file of this project, it calculates the connecting levels between elements in the triple

train: literally, feed and train our model

plot result: literally as well, produce the graph for our report

After receiving suffcient result->

We can write the report and prepare the presentation(at late part of May)